# Archived Config

Deprecated configuration templates are stored here for reference only.

Active runtime configuration now uses:

- `.env.example` as the canonical editable template
- `src/liquidationheatmap/settings.py` for Python runtime settings
- `scripts/lib/runtime_env.sh` for shell wrapper settings
