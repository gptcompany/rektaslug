# TODO - 2026-03-01

## Stato corrente

- **P1.1 ETH/USDT**: Completato (commit `789ce41`)
  - Tier config, snapshots 5290 righe, frontend selector 4 pagine, F1 91.4%, 1215 test passing
- **Gate 1 SUPERATO**: ETH hit rate 91.4% >> threshold 70% -> procedi a P2
- **Prossimo nella roadmap**: P2.2 Historical Backtesting (2-3 giorni) oppure P2.3 Alert System (3-4 giorni)

---

## Debito tecnico da risolvere (priorita ALTA)

### 1. Fix `save_snapshot` schema mismatch
**File**: `src/liquidationheatmap/ingestion/db_service.py` righe 1148-1205
**Problema**: Il metodo `save_snapshot()` prova a inserire `consumed_volume` ma la tabella `liquidation_snapshots` non ha questa colonna. Schema reale: `id, timestamp, symbol, price_bucket, leverage_tier, side, active_volume, density, model, current_price`.
**Impatto**: Il precompute_heatmap.py fallisce al 98% (genera snapshot ma non li salva).
**Fix**: Allineare INSERT in `save_snapshot()` allo schema reale della tabella.

### 2. Ingestione klines_5m ETH insufficiente
**Stato**: Solo 2304 righe (1 settimana: 2026-02-19 a 2026-02-26)
**Confronto**: klines_15m ha 126K righe (5 anni). klines_5m solo ultima settimana.
**Impatto**: Il `precompute_heatmap.py` con intervalli diversi da 15m non funziona per ETH (usa klines_5m).
**Fix**: Eseguire `scripts/ingest_klines_5m.py --symbol ETHUSDT` per coprire almeno 90 giorni, oppure aggiornare `precompute_heatmap.py` per usare klines_15m come fallback.

---

## P1.1 residui (priorita MEDIA)

### 3. Validazione visuale Coinank ETH
**Dipendenza**: Screenshot N8N in `/media/sam/1TB/N8N_dev/screenshots/` con pattern `coinglass_eth_*.png`
**Azione**: Verificare se N8N cattura gia screenshot ETH. Se no, configurare workflow N8N.
**Comando**: `uv run python scripts/validate_screenshots.py --screenshots /media/sam/1TB/N8N_dev/screenshots/ --symbol ETHUSDT --summary`

### 4. Color scheme per simbolo (opzionale)
**Dalla roadmap**: ETH = viola (#627eea), BTC = arancione (#f7931a) nei grafici frontend.
**File**: `frontend/coinglass_heatmap.html`, `frontend/liquidation_map.html`, `frontend/liq_map_1w.html`, `frontend/heatmap.html`
**Note**: Nice-to-have, non bloccante.

---

## Prossime feature dalla roadmap (priorita per ROI)

### 5. P2.2 Historical Backtesting Interface
**Effort**: 2-3 giorni | **User Value**: 5/10 | **Risk**: LOW
**Spec**: `specs/009-eth-symbol/` (riuso 90% da validation pipeline)
**Tasks principali**:
- Estrarre logica backtest da `validate_vs_coinglass.py` in `src/backtesting/engine.py`
- Endpoint POST `/backtest/run` con strategy params
- Frontend `backtest.html` con equity curve (Plotly.js)
- CSV export

### 6. P2.3 Liquidation Zone Alerts
**Effort**: 3-4 giorni | **User Value**: 6/10 | **Risk**: LOW
**Spec**: `specs/010-alert-system/`
**Tasks principali**:
- Tabella `alerts` in DuckDB
- CRUD endpoints `/alerts`
- Background monitor (poll prezzo ogni 10s)
- Webhook + email notifications

### 7. P2.1 Real-time Streaming
**Effort**: 5-7 giorni | **User Value**: 9/10 | **Risk**: MEDIUM
**Spec**: `specs/011-realtime-streaming/`
**Tasks principali**:
- WebSocket infrastructure (Binance + Hyperliquid)
- Redis pub/sub
- `/ws/liquidations` endpoint
- Frontend auto-reconnect

---

## Suggerimento sequenza per domani

1. **Fix save_snapshot** (~30 min) - debito tecnico bloccante
2. **Fix klines_5m ETH** (~1h wait time) - completamento dati
3. **Valuta Gate 1**: Decidere se iniziare P2.2 (backtesting) o P2.3 (alerts)
4. **Inizia prossima feature**: Setup spec, plan, primi task
