# Legacy Frontend Archive

This folder contains archived frontend implementations kept only as reference.

The root `frontend/` directory now keeps only the active pages plus lightweight compatibility wrappers for the legacy paths.

Archived files in this folder should not be treated as active implementation targets.
