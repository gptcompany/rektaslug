# LiquidationHeatmap Architecture

The canonical architecture document lives in `docs/ARCHITECTURE.md`.

Use that file as the single source of truth for:

- system architecture
- component responsibilities
- data flow
- runtime configuration model

The previous root-level placeholder architecture template has been archived under `archive/docs/ARCHITECTURE.placeholder.md`.
